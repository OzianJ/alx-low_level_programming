#include "main.h"
/**
 * _abs - entry point
 * Description 'returns the absolute value of intigers'
 *@a: the intiger to be returned
 * return: 0 if successful
 * Return: Void.
 */
int _abs(int a)
{
if (a < 0)
{
return (a * -1);
}
return (a);
}
