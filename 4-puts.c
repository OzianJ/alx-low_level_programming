#include <stdio.h>
/*
 *puts- a library function to send formatted outputs to the screen
 *int main(void)- the entry point of the function
 *Return- exit status of the program which returns 0 if successful
 */
int main(void)
{
  puts("\"Programming is like building a multilingual puzzle\n");
  return (0);
}
