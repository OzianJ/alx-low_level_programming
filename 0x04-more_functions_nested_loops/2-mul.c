#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 * Description: 'multiply two digits'
 * @a: first digit
 * @b: second digit
 * Return: Always 0.
 */

int mul(int a, int b)
{
  return (a * b);
}
