#include "main.h"
#include <stdio.h>

/**
 * mul - entry point
 * Description: 'multiply two digits'
 * @a: first intiger
 * @b: second intiger
 * Return: Always 0.
 */

int mul(int a, int b)
{
return (a * b);
}
